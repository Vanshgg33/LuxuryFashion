# ---------- APP ----------
spring.application.name=LUXURYFASHION
server.port=${PORT:8081}
server.address=0.0.0.0
app.frontend.url=${FRONTEND_URL:http://localhost:5173}
app.backend.url=${BACKEND_URL:http://localhost:8081}

# ---------- DataSource ----------
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/projects}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:VANSH}


# ---------- JPA / HIBERNATE ----------
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ---------- JWT ----------
jwt.secret=${JWT_SECRET:8f7d3b92e61a4c5f0d8e9c7b6a5f4e3d2c1b0a9876543210fedcba9876543210}
jwt.expiration=${JWT_EXPIRATION:1800000}

# ---------- MAIL ----------
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:studym365@gmail.com}
spring.mail.password=${MAIL_PASSWORD:reqv dusu szvz acmz}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ---------- GOOGLE OAUTH ----------
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:966552916808-4hddqbhoitg9r90tprisnivjp8pmca6o.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
spring.security.oauth2.client.registration.google.redirect-uri=${BACKEND_URL:http://localhost:8081}/login/oauth2/code/google
spring.security.oauth2.client.registration.google.scope=profile,email

# ---------- UPLOADS ----------
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB
product.picture.path=C:/Users/jaisw/OneDrive/Desktop/Moon/ProdouctImages
profile.picture.path=C:/Users/jaisw/OneDrive/Desktop/Moon/ProfileImages

# ---------- SESSION ----------
server.servlet.session.cookie.same-site=None
server.servlet.session.cookie.secure=true
