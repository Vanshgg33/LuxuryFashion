# ---------- APP ----------
spring.application.name=LUXURYFASHION
server.port=${PORT:8081}
server.address=0.0.0.0
app.frontend.url=${FRONTEND_URL:http://localhost:5173}
app.backend.url=${BACKEND_URL:http://localhost:8081}

# ---------- DataSource ----------
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# Cloud SQL Connection - Public IP (Connection Name: rangeelaboutique:asia-south1:rangeela)
# Public IP: 34.14.181.133, Port: 3306
# Using public IP connection (Public IP connectivity is Enabled)
# IMPORTANT: Make sure your IP is whitelisted in Cloud SQL authorized networks
# SSL is REQUIRED (Cloud SQL instance is set to "Allow only SSL connections")
# Try different connection strings if this doesn't work:
# Option 1 (Current): Direct IP with SSL
spring.datasource.url=${DATABASE_URL:jdbc:mysql://34.14.181.133:3306/rangeela?useSSL=true&requireSSL=true&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata&useUnicode=true&characterEncoding=UTF-8&verifyServerCertificate=false&useLegacyDatetimeCode=false&connectTimeout=30000&socketTimeout=60000}
# Option 2 (Alternative - try if Option 1 fails): Without requireSSL
# spring.datasource.url=${DATABASE_URL:jdbc:mysql://34.14.181.133:3306/rangeela?useSSL=true&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata&useUnicode=true&characterEncoding=UTF-8&verifyServerCertificate=false&connectTimeout=30000&socketTimeout=60000}

spring.datasource.username=${DATABASE_USERNAME:admin}
spring.datasource.password=${DATABASE_PASSWORD:40Lpa123.}

# Connection Pool Settings
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=900000
spring.datasource.hikari.leak-detection-threshold=60000
# Prevent connection drops
spring.datasource.hikari.keepalive-time=30000


# ---------- JPA / HIBERNATE ----------
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# Use 'update' to create/update tables without dropping existing data
# Use 'create' only for initial setup (WARNING: This will DROP existing tables and data!)
# Use 'validate' for production to only validate schema without making changes
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
# Hibernate properties
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# ---------- JWT ----------
jwt.secret=${JWT_SECRET:8f7d3b92e61a4c5f0d8e9c7b6a5f4e3d2c1b0a9876543210fedcba9876543210}
jwt.expiration=${JWT_EXPIRATION:432000000}

# ---------- MAIL ----------
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:rangeelaboutique6@gmail.com}
spring.mail.password=${MAIL_PASSWORD:fluz crnk svpq tiww}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ---------- GOOGLE OAUTH ----------
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:381761138-uknjvjphmul1vrh2pot9h654bju53hiu.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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
spring.security.oauth2.client.registration.google.redirect-uri=${BACKEND_URL:http://localhost:8081}/login/oauth2/code/google
spring.security.oauth2.client.registration.google.scope=profile,email

# ---------- UPLOADS ----------
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
product.picture.path=C:/Users/jaisw/OneDrive/Desktop/Moon/ProdouctImages
profile.picture.path=C:/Users/jaisw/OneDrive/Desktop/Moon/ProfileImages

# ---------- GOOGLE CLOUD STORAGE ----------
gcs.bucket.name=${GCS_BUCKET_NAME:productpotos}
gcs.project.id=${GCS_PROJECT_ID:rangeelaboutique}
# Set GOOGLE_APPLICATION_CREDENTIALS environment variable to path of service account JSON file
# Or use default credentials if running on GCP

# ---------- SESSION ----------
server.servlet.session.cookie.same-site=None
server.servlet.session.cookie.secure=true

# ---------- RAZORPAY PAYMENT ----------
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_RdcgBs8hLIAVc7}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:ri0fgXSH0mJKvSMM0Kjtf7xN}
razorpay.webhook.secret=${RAZORPAY_WEBHOOK_SECRET:your_webhook_secret_here}
